import 'package:flutter/material.dart';

class DioResultPage extends StatelessWidget {
  const DioResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dio, Retrofit Ex'),
      ),

      body: Center(

      ),
    );
  }
}
