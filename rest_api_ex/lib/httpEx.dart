import 'package:flutter/material.dart';

class HttpEx extends StatelessWidget {
  const HttpEx({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Http ex'),
      ),
    );
  }
}
